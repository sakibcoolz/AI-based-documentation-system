# Markdown Rules

- Use a single `#` heading per document.
- Prefer short sections with stable anchors.
- Use fenced code blocks for commands, JSON, Mermaid, and API examples.
- Use repository-relative links inside `docs/`.
- Keep requirements normative with `must`, `should`, and `may`.
- Record assumptions explicitly instead of implying certainty.
