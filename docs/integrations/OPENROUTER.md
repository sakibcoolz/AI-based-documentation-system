# OpenRouter Integration

## Purpose
Provide model access through aliases instead of hardcoded model IDs.

## Required Aliases
- `fast`
- `code`
- `reasoning`
- `documentation`
- `reviewer`

## Operational Requirements
Document retries, fallbacks, timeouts, token usage, and cost tracking.
